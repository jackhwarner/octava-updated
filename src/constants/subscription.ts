
/**
 * If true, users without an active subscription are redirected to the subscription page.
 * If false, subscription checks are not enforced and users are not redirected.
 */
export const REDIRECT_IF_NO_SUBSCRIPTION = false;
